# Test Plan
Generated by /plan-eng-review on 2026-06-22
Branch: (no git, local dev)
Repo: vtrip (携程数据迁移助手 Chrome Extension)

## Affected Pages/Routes
- `/ivbk/vendor/baseInfoMerge` — 提取快照基线建立后，验证 _extractFormItem 等纯函数对 mixed-group/select-group/input-number-group 等复合行字段的提取等价性
- `/product/input/productImageText` — 待 HTML 样本提供后建立快照基线，验证 recommendReason/richText 提取
- 任何新页面（场景1） — 验证新 adapter.js 注册后，老页面快照测试零回归
- 任何新页面（场景2，新字段类型） — 验证新 handler 的 detect 不误命中老类型 form-item

## Key Interactions to Verify
- 阶段1：page-adapters.js 拆分后，baseInfoMerge 国内版导出 JSON 结构不变（version/source/tab/data）
- 阶段1：productImageText 国内版导出，推荐理由 + 产品特色提取正常
- 阶段1：国际版导入，复合行字段（mixedGroup/selectGroup）行级定位填充正常
- 阶段2：Registry 查询插入后，无类型注册时全走老路径，行为零变化
- 阶段3：Sanitizers 抽出后，AI 改写国际版字符清洗结果与原 _sanitizeForInternationalRules 一致
- 阶段3：富文本 imageid 统一为 41973044 后保存不丢内容

## Edge Cases
- baseInfoMerge 页面：隐藏 .ant-form-item-hidden 占用 DOM index 导致匹配预览字段错位（独立计数器）
- baseInfoMerge 页面：label 无 for 属性时走 input#id → closest(.ant-select) 兜底
- productImageText 页面：富文本填入后保存为空（UEditor 主世界桥接必须生效）
- productImageText 页面：推荐理由分类+描述子字段智能匹配（_smartMatchSubField）
- SPA 路由切换：从 baseInfoMerge 切到 productImageText，PageRegistry.activate 必须卸载前者专属字段
- 新 handler detect：用老页面 fixture 反向验证不误命中老类型

## Critical Paths
- 国内版 baseInfoMerge 导出 → 剪贴板 JSON → 国际版粘贴 → 转换预览 → 匹配预览 → 确认填写 → 回读验证（复合行字段全链路）
- 国内版 productImageText 导出 → 国际版导入 → 推荐理由分类+描述填入 + 富文本 UEditor 保存不丢内容
- 新页面开发（场景1）：新建 adapter.js + register + manifest matches → 老页面快照零回归 + 新页面手动验证
